# Input Builder Sync Policy

- Keep `ccfep_workstream_input` compatible with `ccfep-io-contracts`.
- Route resource location changes to `ccfep-resource-catalog`.
- Route new shared object fields to `ccfep-io-contracts`.
