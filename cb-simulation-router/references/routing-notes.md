# Routing Notes

- `dynamics_branch` routes into `dynamics_dos` executor family
- `press_temp_fillin` routes into rerun or fill-in simulation handling through manifest or executor selection
- `simulation_rerun` and `simulation_status` stay operational and should not be confused with LI or thermo second-stage analysis
