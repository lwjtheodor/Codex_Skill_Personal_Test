# Template Families

- `manifest`
- `report`
- `handoff`
- `remote_job`
- `workstream_local_task`

Each family should resolve to a template object plus a machine-readable `template_resolution_result`.
