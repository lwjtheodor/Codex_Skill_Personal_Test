# Template Registry Sync Policy

- Keep template families stable and additive.
- Treat template files as resources; treat registry entries and resolution results as shared objects.
- Do not place source-path mapping here.
- Route missing template snapshots to `ccfep-resource-catalog`.
- Route implementation support changes to `ccfep-workstream-coder`.
