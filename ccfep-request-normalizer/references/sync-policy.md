# Request Normalizer Sync Policy

- Keep request object fields stable and additive.
- Route resource discovery changes to `ccfep-resource-catalog`.
- Route shared object shape changes to `ccfep-io-contracts`.
